# Font source packages — only the shipped woff2 lives in fonts/
Panchang_Complete/
Sprat-type-master/

# Source media — only encoded output in media/ is committed
*.mov
ascii-magic-*.mp4
locked-off_camera_motion_only_in_the_clouds-*.mp4

# Source art. Each of these is the input to something small in media/:
#   Sprite.png       (1.7 MB) -> media/neel-sprite.png   (11 KB)
#   folder_icon2.png (4.0 MB) -> media/folder.png        (209 KB)
# Committing them would put 10 MB in the repo to ship 215 KB.
/Sprite.png
/Folder_icon.png
/folder_icon2.png
/folder.png

# Design source files
*.psd
*.ai
*.fig
*.sketch

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Unused font cut — Sprat ships as one weight (ExtendedLight) only
fonts/Sprat-ExtendedBold.woff2

# Calfine source (only the shipped woff2 is committed)
calfine/
calfine.zip
